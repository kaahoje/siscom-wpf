@using RestauranteMobile.Extensions
@model RestauranteMobile.Models.PedidoModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <a href="@Url.Action("NovoPedido", "Pedido", new { mesa = Model.Entity.Mesa })">
            <img src="~/Images/novoPedido.png" />
        </a>
        <a href="@Url.Action("Index","Pedido")" ><img src="~/Images/voltar.png"/></a>
    </div>
    @{
        Html.RenderPartial("_PedidoPartialPage", Model);

        Html.RenderPartial("_TotaisPedido", Model);

        Html.RenderPartial("_ProdutoPedidoPartialPage", Model);
    }

</div>
