@model List<int>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@foreach (var m in Model)
{
    
    <div class="btn btn-lg btn-default" style="text-decoration: none; ">
            <a href="@Url.Action("Consumo","Pedido",new {mesa= m})">
                @(("0" + m).Substring(("0" + m).Length - 2))
            </a>
            @*@Ajax.Link(("0" + m).Substring(("0" + m).Length - 2),
                 "NovoPedido", "Pedido", new { mesa = m })
            @Html.Link(("0" + m).Substring(("0" + m).Length - 2),
            @Url.Action("ConsumoMesa","Pedido", new { mesa = m }))*@
        </div>

}